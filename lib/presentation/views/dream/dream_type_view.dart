import 'package:flutter/material.dart';

class DreamTypeView extends StatelessWidget {
  static const name = 'dream_type_view';
  const DreamTypeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}