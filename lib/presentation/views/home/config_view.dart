import 'package:flutter/material.dart';

class ConfigView extends StatelessWidget {
  static const name = 'config_view';
  const ConfigView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Config View'),
    );
  }
}
